# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Data processing
numpy>=1.26.0
pandas>=2.2.0

# Time-series DB
influxdb-client>=1.44.0

# Industrial protocols
asyncua>=1.1.0
pymodbus>=3.7.0

# Security & utilities
cryptography>=43.0.0
python-multipart>=0.0.12
httpx>=0.27.0

# Reporting (FR-22)
reportlab>=4.2.0

# Dev & test
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.7.0

# Install ML stack separately (large download):
#   pip install -r requirements-ml.txt

# Docker Desktop must be running for InfluxDB:
#   docker compose up -d influxdb
